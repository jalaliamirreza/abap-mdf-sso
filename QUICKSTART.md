# شروع سریع
# Quick Start Guide

این راهنما به شما کمک می‌کند در کمتر از 15 دقیقه سیستم را راه‌اندازی کنید.

This guide will help you get the system up and running in less than 15 minutes.

---

## پیش‌نیازها / Prerequisites

✅ دسترسی به سیستم SAP S/4HANA
✅ Python 3.8+ نصب شده
✅ دسترسی به سایت تامین اجتماعی

---

## 5 قدم تا اجرا / 5 Steps to Run

### قدم 1️⃣: Clone و نصب

```bash
# Clone repository
git clone https://github.com/your-org/abap-mdf-sso.git
cd abap-mdf-sso

# ایجاد virtual environment
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# یا
venv\Scripts\activate     # Windows

# نصب وابستگی‌ها
pip install -r requirements.txt
```

⏱️ **زمان**: 3 دقیقه

---

### قدم 2️⃣: تنظیم SAP

```bash
# کپی فایل تنظیمات
cp config/sap-config.template.json config/sap-config.json

# ویرایش تنظیمات (نام کاربری و رمز عبور خود را وارد کنید)
nano config/sap-config.json
```

```json
{
  "sap_connection": {
    "host": "your-sap-server.com",
    "system_number": "00",
    "client": "100",
    "user": "YOUR_USERNAME",
    "password": "YOUR_PASSWORD",
    "language": "FA"
  }
}
```

⏱️ **زمان**: 2 دقیقه

---

### قدم 3️⃣: آپلود برنامه ABAP

1. فایل `src/abap/ZHCM_SSO_EXTRACT.abap` را باز کنید
2. وارد SAP شوید
3. تراکنش `SE38` را اجرا کنید
4. برنامه جدید `ZHCM_SSO_EXTRACT` ایجاد کنید
5. کد را کپی/پیست کنید
6. Activate کنید (Ctrl+F3)

⏱️ **زمان**: 5 دقیقه

---

### قدم 4️⃣: استخراج داده از SAP

در SAP:

```
تراکنش: SE38
برنامه: ZHCM_SSO_EXTRACT

پارامترها:
  شماره پرسنل: 00000001 تا 99999999
  سال: 1402
  ماه: 01
  نوع خروجی: J (JSON)

F8 (اجرا)
```

فایل JSON را دانلود کنید.

⏱️ **زمان**: 3 دقیقه

---

### قدم 5️⃣: تولید DBF و آپلود

```bash
# تولید DBF
python src/utils/generate_dbf.py \
  --input /path/to/sap_output.json \
  --output MADRAK_140201.DBF

# بررسی نتیجه
ls -lh MADRAK_140201.DBF
```

سپس:
1. به سایت https://www.tamin.ir بروید
2. بخش کارفرمایان > لیست ماهانه بیمه
3. فایل DBF را آپلود کنید

⏱️ **زمان**: 2 دقیقه

---

## ✅ تست با داده نمونه

قبل از اجرای واقعی، با داده‌های نمونه تست کنید:

```bash
# اجرای تست‌ها
python tests/test_dbf_generation.py

# تولید DBF از داده نمونه
python src/utils/generate_dbf.py \
  --input tests/sample_data.json \
  --output test_output.dbf
```

اگر تست‌ها موفق بودند، سیستم آماده استفاده واقعی است! ✨

---

## 🆘 مشکل دارید?

### Python نصب نیست

```bash
# Ubuntu/Debian
sudo apt install python3 python3-pip

# macOS
brew install python3

# Windows
# از python.org دانلود کنید
```

### خطای "Module not found"

```bash
pip install -r requirements.txt
```

### برنامه ABAP خطا می‌دهد

بررسی کنید:
- دسترسی‌های لازم را دارید؟
- Infotype ها موجود هستند؟
- Wage Types صحیح است؟

### فایل DBF قبول نمی‌شود

بررسی کنید:
- نام فایل: `MADRAK_YYYYMM.DBF`
- کدگذاری: Windows-1256
- فیلدهای الزامی پر شده‌اند

---

## 📚 مستندات کامل

برای جزئیات بیشتر:

- [راهنمای نصب کامل](docs/INSTALLATION.md)
- [راهنمای استفاده](docs/USAGE.md)
- [مشخصات فرمت](docs/SSO_FORMAT_SPEC.md)

---

## 🎯 مراحل بعدی

بعد از راه‌اندازی موفق:

1. ✅ Backup خودکار تنظیم کنید
2. ✅ Job ماهانه در SAP ایجاد کنید
3. ✅ گزارش‌گیری راه‌اندازی کنید
4. ✅ تیم را آموزش دهید

---

## 💡 نکات مهم

⚠️ **امنیت**: فایل `sap-config.json` را هرگز commit نکنید

💾 **Backup**: همیشه قبل از ارسال، backup تهیه کنید

📅 **زمان‌بندی**: لیست بیمه را تا پایان ماه بعد ارسال کنید

🔍 **بررسی**: قبل از ارسال نهایی، داده‌ها را بررسی کنید

---

## 🎉 موفق باشید!

اکنون سیستم شما آماده است. برای کمک بیشتر:

- 📧 Email: support@company.com
- 📞 Phone: 1536
- 🐛 Issues: GitHub Issues

---

**نسخه**: 1.0
**آخرین بروزرسانی**: 1403/11/01
